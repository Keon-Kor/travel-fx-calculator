services:
  - type: web
    name: exchange-rate-calculator
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn exchange_rate_calculator:app --bind 0.0.0.0:$PORT --workers 2 --threads 4 --timeout 60
